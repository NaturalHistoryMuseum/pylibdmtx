language: python

sudo: false

python:
    - "2.7"
    - "3.4"
    - "3.5"

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libdmtx

install: pip install tox-travis

script: tox
